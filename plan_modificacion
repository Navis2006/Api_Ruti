Rol: Eres un Arquitecto de Software y Desarrollador Backend Senior experto en Node.js (Express.js), bases de datos SQL relacionales (MySQL) y consumo de APIs de geolocalización de terceros.

Contexto del Proyecto ("Rutitruck"):

Objetivo: Desarrollar el backend para una aplicación móvil de logística (Flutter) que traza rutas seguras y legales exclusivamente para vehículos de carga pesada (tráileres).

Estado Actual: Existe una web administrativa en PHP puro. La base de datos es MySQL alojada en Clever Cloud. Actualmente existe una carpeta api/ en PHP, pero la directiva es migrar toda esa lógica de la API a un nuevo microservicio en Node.js (Express.js) que se alojará en Render.

Misión Inmediata: 1.  Estructurar el nuevo servidor Express.js para que reemplace a la antigua API de PHP, conectándose de forma independiente a la misma base de datos en Clever Cloud.
2.  Implementar un endpoint fundamental que consuma la Routing API de TomTom para calcular rutas de vehículos pesados y almacene la geometría resultante en la base de datos.

Nota sobre la Base de Datos: A continuación de este prompt, te proporcionaré el script SQL completo del respaldo de la base de datos actual. Debes usarlo para conocer los nombres exactos de las tablas y campos antes de generar el código.

Especificaciones Técnicas y Tareas de Ejecución:

Fase 1: Setup y Arquitectura del Microservicio (Migración a Express)
Inicialización: Crea la estructura base de un proyecto Express.js modular (patrón de Controladores y Rutas separados).

Dependencias Obligatorias: express, cors (configurado para permitir peticiones de la app móvil), dotenv (gestión de variables de entorno), mysql2 (usando la API de Promesas para conexiones asíncronas) y axios (para llamadas HTTP a TomTom).

Fase 2: Conexión a Base de Datos (Clever Cloud) y Migración de Esquema
Pool de Conexiones: Crea el código para config/db.js que exporte un pool de conexiones de mysql2/promise. Este es el nuevo "puente" independiente del microservicio hacia Clever Cloud. Debe inyectar las variables: DB_HOST, DB_USER, DB_PASSWORD, DB_NAME, DB_PORT.

Adaptación del Esquema SQL: Analiza el script SQL que te enviaré. Identifica la tabla lógica que maneja las rutas o viajes asignados a los operadores. Escribe el comando ALTER TABLE exacto para agregar una nueva columna (ej. coordenadas_tomtom) de tipo JSON o LONGTEXT donde guardaremos las rutas generadas.

Fase 3: Endpoint de Ruteo Inteligente (TomTom API)
Construye el controlador (controllers/routeController.js) para generar la ruta del camión (POST /api/rutas/generar). El flujo lógico estricto debe ser:

Recepción de Payload: El endpoint espera un JSON en el req.body con los identificadores necesarios según mi esquema SQL, más las coordenadas de origen y destino (lat, lng).

Petición a TomTom (Axios):

Armar la URL dinámica hacia: https://api.tomtom.com/routing/1/calculateRoute/{latOrigen},{lngOrigen}:{latDestino},{lngDestino}/json

Query Parameters Críticos: * key: process.env.TOMTOM_API_KEY

travelMode: "truck"

vehicleWeight: 30000

vehicleHeight: 4.15

Extracción y Transformación de Datos:

No devolver el JSON crudo al frontend. Extraer estrictamente el arreglo de coordenadas ubicado en: response.data.routes[0].legs[0].points.

Persistencia (MySQL):

Convertir el arreglo de points a un string JSON válido.

Ejecutar un UPDATE en la tabla correspondiente de mi script SQL para guardar esta ruta.

Respuesta al Cliente (Flutter):

Retornar HTTP Status 200 con un JSON estructurado que contenga success: true y el arreglo limpio de coordenadas en la propiedad data.

Entregables esperados por tu parte:

Código de server.js.

Código de config/db.js.

Código del controlador de TomTom (controllers/routeController.js).

El comando SQL exacto (ALTER TABLE) basado en mi script.